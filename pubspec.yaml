name: edge_exit_interceptor
description: Internal Flutter package for custom edge-exit feedback and guarded page dismissal on selected routes.
version: 0.0.1

environment:
  sdk: ^3.11.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
flutter:
